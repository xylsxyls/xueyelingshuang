#pragma once

class CCheckScreen{
public:
	//检测当前区域是否有本地图片，相似度，检测次数，间隔时间
	//检测当前区域是否有文字，相似度，字库路径，检测次数，间隔时间
	//检测当前区域是否有指定颜色，相似度，检测次数，间隔时间
	//后台检测当前区域图片，相似度，间隔时间，当出现时进入虚函数，是否循环
	//后台检测当前区域文字，相似度，字库路径，间隔时间，当出现时进入虚函数，是否循环
	//后台检测当前区域指定颜色，相似度，间隔时间，当出现时进入虚函数，是否循环
};