如果两个类相互包含，
不可以在.h中直接用另一个类定义成员变量，
可以在.h中用另一个类定义指针
可以在.h中用另一个类作为函数入参
在.h中有实现的且入参为另一个类的函数，如果入参为指针，且使用时没有用到指针所指成员可以，用到不行，如果入参为类对象，用不到也不行
在.h中任何有实现的函数里不可以使用另一个类定义对象
.cpp中没有任何限制
.h中有class A;class B;声明即可
.h中需要有#pragma once

如果不是相互包含，那么一个类中有另一个类的对象作为成员也是可以的