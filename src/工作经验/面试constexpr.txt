constexpr是C++11引入的关键字，用于在编译时求值的表达式。它可以用于函数、变量、和构造函数，以及一些简单的控制流结构。

函数的参数和返回值类型都必须是字面量类型（literal types），这通常包括基本数据类型和一些用户定义的字面量类型。

函数体内只能包含一些简单的计算，如算术运算、条件语句等。在C++14及以后的版本中，这方面的限制有所放宽，但仍然有一些约束。

这样，当你使用constexpr修饰一个函数时，编译器就知道可以在编译时计算函数的值，而不必等到程序运行时才计算。这对于一些需要在编译时就确定的常量计算非常有用，可以提高程序的性能和效率。