无论是CString通过GetBuffer()还是string通过c_str()转char*之后如果直接=，那么是把类中的字符串首地址导出来复制到定义的char*里，所以当char*改变之后类里的字符串也会改变，所以尽量使用strcpy函数，以免相互影响。

当变成char数组时，如果能确保变成的char数组不会被改变，那么可以直接使用，往往是作为一些系统函数的入参
而如果变成char数组之后需要对这个数组进行变化修改的话，那么应使用strcpy函数进行深拷贝。

传给的char*必须是char数组的首地址，否则会因为申请内存不足发生越界访问。