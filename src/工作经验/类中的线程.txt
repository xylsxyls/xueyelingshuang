线程如果开在类的内部，一旦类被释放，线程里传入this指针访问到的数据就是随机值，所以，在使用这种类的时候必须保证在子线程存在时类必须一直存在，但如果线程里访问到的类内数据会被static保护起来的话则不会出现该问题

static void _stdcall FunThread();这种定义类中线程函数的方式是错误的，实际则是顺序运行不能并行，必须采用CreateThread开线程，所有传入线程内使用的东西都必须不依赖于类，也不可以是局部变量的地址。