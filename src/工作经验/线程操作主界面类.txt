线程可以将一些会卡死界面的长时间操作放到后台运行
但是主界面类会随时改变成员变量的值，所以，线程不可以读成员变量的值也不可以写
方法是，先将需要传入线程的成员变量值放入new出的空间内，然后把这个空间的地址传入线程
如果有多个值，可以new一个vector<void *>，然后把各个new出的空间放到这个向量里，再把向量的地址传入线程中
线程操作主界面类的方式是，通过给主界面发送消息，主界面在接收到消息之后会触发自己的成员函数从而执行需要的操作
线程传值给主界面类可以直接通过postmessage里的参数传
对应的地方需要加delete