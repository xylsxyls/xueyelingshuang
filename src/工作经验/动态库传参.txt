如果在主函数里写了一个函数指针，要求动态库里的函数地址给这个指针，
动态库里的形参必须一致，原理是主函数内传入的形参会直接给动态库里的形参列表赋值，比如
主函数里的int = 300，赋值给形参里的char之后，形参里的char为44，但是还是会一一赋值，直到形参列表被完全赋值完毕
返回值与上同理，也是把子函数里需要返回的值赋值给主函数里接的变量。
但是只要函数名对应了就会执行函数体。

无论动态库还是共享库都不支持重载