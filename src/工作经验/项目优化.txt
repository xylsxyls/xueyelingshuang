递归获得该目录底下的所有路径，过滤出.c.cpp.h.inl
打开文件，获得其中
输入框中默认输入，密码.//?.c.cpp.h.inl，如有改变才输入，默认为空
检测//?，取出上面两行以及当前行存入note.txt

默认文件名唯一
导入先以第一行输入的密码进行解密
对比源文件，定义几个计数指针i，分别和.note文件相比，如果两者完全相同则把第三行插入，如果访问完一遍文件之后依然找不到则存入log.txt，不加密标明文件
换行符号<line break>后面一行为文件名